# Server
PORT=4000
# Comma-separated list of allowed origins for CORS (examples shown)
#CORS_ORIGIN=http://localhost:5173,http://127.0.0.1:5173

# Auth
JWT_SECRET=change_me_to_a_long_random_string

# Database (choose ONE approach)

# Option A: Single connection URL
# Format: postgres://USER:PASSWORD@HOST:PORT/DBNAME
#DATABASE_URL=postgres://postgres:postgres@192.168.1.50:5432/storedb

# Option B: Individual fields
PGHOST=192.168.1.50
PGPORT=5432
PGDATABASE=storedb
PGUSER=postgres
PGPASSWORD=postgres

# SSL mode for Postgres connections
#   require  -> use SSL (common on cloud providers)
#   disable  -> no SSL (typical for local/LAN)
PGSSLMODE=disable

# Seed script
# Admin password that will be inserted/updated when running `npm run seed`
ADMIN_PASSWORD=admin123
